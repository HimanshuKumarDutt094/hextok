<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Allow querying for document/open intents on Android 11+ to ensure the picker intent is resolvable -->
    <queries>
        <intent>
            <action android:name="android.intent.action.OPEN_DOCUMENT" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.OPENABLE" />
            <data android:mimeType="*/*" />
        </intent>
    </queries>

    <!-- Allow network access to fetch dev server bundle -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Storage permissions: READ_EXTERNAL_STORAGE for older Android; READ_MEDIA_* for Android 13+ -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- Allow cleartext HTTP traffic for development server (dev only) -->
    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hextok"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Hextok"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- Deep link intent filter for OAuth callback -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="hextok" />
            </intent-filter>
        </activity>
        
        <!-- File picker activity used by FilePicker native module -->
        <activity
            android:name="com.hextok.filepicker.FilePickerActivity"
            android:exported="false"
            android:theme="@style/Theme.Hextok" />
        <!-- Permission request activity used by FilePermissionModule -->
        <activity
            android:name="com.hextok.filepicker.PermissionRequestActivity"
            android:exported="false"
            android:theme="@style/Theme.Hextok" />
    </application>

</manifest>